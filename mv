The 'mv' command is used to rename (or 'move') files from its source in the first parameter, to the path specified in its second parameter.
For example:
mv myFile ../myFile

It may also be used to rename files within the same directory.
For example:
mv oldFile newerFile
